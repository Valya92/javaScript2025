function project(name,projectNum){
    
    let timeNeeded = projectNum * 3;
    console.log(`The architect ${name} will need ${timeNeeded} hours to complete ${projectNum} project/s.`);
}

project("George",

4);